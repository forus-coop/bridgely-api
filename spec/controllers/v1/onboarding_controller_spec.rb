require 'spec_helper'

describe V1::OnboardingController do
  before :each do
  end
   
  context "receives sms message from twilio" do
    before :each do
      # fake an incoming sms from twilio
    end
     
    it "responds to the message with onboarding sms messages" do
      # fake some outgoing sms messages to twilio
    end 
  end 
end
